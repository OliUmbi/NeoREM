import {Heading, Section, Stack, Switch} from "@carbon/react";

const Users = () => {

    return (
        <Stack spacing={8}>
            <Heading>User</Heading>
        </Stack>
    )
}

export default Users
